// c file 
#include <stdio.h>

int main() {
   /* my first program in C */
   
   

}


// a pretty bad and slow algorhitm to get the grosster gemeinsamer teiler 
int biggest_together_divisor(int a, int b){
   
   int n_bigger = a;
   int n_smaller = b; 
   if(b > a){
      n_bigger = b; 
      n_smaller = a; 
   }

   int is_divisor_of_a = 0;
   int is_divisor_of_b = 0;

   int biggest_together_divisor = n_smaller;

   while(1){

      int remainder_a = 

      if(is_divisor_of_a && is_divisor_of_b){
         
         break;
      }

      biggest_together_divisor --; 
   }

}